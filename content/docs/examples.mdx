---
title: Examples
description: Text drafts, selections, and a custom editor you can adapt.
---

## Text with Enter

<TextFilterDemo />

The parent owns the applied tags and editor open state. Enter parses the draft, stores it, and closes the editor. Shift+Enter inserts a line. Escape or outside dismissal unmounts the draft without committing it. Validation keeps an empty list from being applied.

<ComponentSource name="text-filter-demo" />

## Single and multiple selection

Open the filter menu and choose Status or Assignee to see the options. Status accepts one choice. Assignee supports search and several selections. Choosing a value applies it and creates a chip. Click that chip to edit it later.

<FiltersDemo />

<ComponentSource name="filters-demo" />

## Custom, required filter

<CustomFilterDemo />

This editor uses a native checkbox. It updates immediately, and it has no remove action. Passing `editor` enables editing independently of `onRemove`.

When placing form fields in a dropdown, stop typing and cursor keys from reaching menu typeahead. Leave Escape available to dismiss the menu. Keep Tab native inside form editors so it moves between controls.

<ComponentSource name="custom-filter-demo" />

## Low-level composition

```tsx
import {
  FilterChip,
  FilterEditor,
  FilterEditorContent,
  FilterEditorTrigger,
  FilterRemove,
} from "@/components/ui/filters";

<FilterEditor open={open} onOpenChange={setOpen}>
  <FilterChip>
    <FilterEditorTrigger aria-label="Edit status filter">Status: {status}</FilterEditorTrigger>
    <FilterRemove aria-label="Remove status filter" onClick={clearStatus} />
  </FilterChip>
  <FilterEditorContent aria-label="Status filter">{editor}</FilterEditorContent>
</FilterEditor>;
```

Here `open`, `setOpen`, `status`, `clearStatus`, and `editor` come from your application. Use the complete examples above for a runnable starting point.
